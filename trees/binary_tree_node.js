// Basic Binary Tree Node that we will need to store data
class Node {
    constructor(data) {
        // Values are stored in the data
        this.data = data;
        // left and right are children nodes that are refered to
        this.left = null;
        this.right = null;
    }
}
